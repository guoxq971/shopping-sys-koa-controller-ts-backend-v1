/* where条件 del + status 可用 */
export const whereConditionByDelAndStatus = ` where 1=1 and is_del = 0 and status = 1 `;
/* where条件 del 可用 */
export const whereConditionByDel = ` where 1=1 and is_del = 0 `;
/* where条件 status 可用 */
export const whereConditionBySttus = ` where 1=1 and is_del = 0 `;
/* where条件 */
export const whereConditionl = ` where 1=1 `;
